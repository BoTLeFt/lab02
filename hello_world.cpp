#include <iostream>
#include <string>

int main(){
    std::string name; //Переменная
    std::sin >> name; //Ввод значения переменной
    std::cout << "Hello world from " << name; //Вывод
    return 0;
}
